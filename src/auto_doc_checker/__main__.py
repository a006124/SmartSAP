from auto_doc_checker.cli import main

if __name__ == "__main__":
    main()

# c'est ce fichier que python chercher quand on exec via python -m auto_doc_checker
# on a pas besoin de mettre ce idiome dans le fichier cli (qui sert de main)
# on devrait si on voulait lancer le fichier directement